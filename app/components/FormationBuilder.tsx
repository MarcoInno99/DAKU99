"use client";
import { useEffect, useMemo, useState } from "react";
import type { Player, Team } from "../data/league";
import { formations } from "../data/league";
type Point={x:number;y:number};type PlayerInfo={probability:number|null};type Availability={updated?:string|null;unavailable?:boolean;players:Record<string,PlayerInfo>};
function eligible(role:string,slot:string){return role.split("/").some(item=>slot.split("/").includes(item))}
function shape(counts:number[]):Point[]{const levels=[72,51,30,10];return[{x:50,y:91},...counts.flatMap((count,row)=>Array.from({length:count},(_,i)=>({x:count===1?50:12+76*i/(count-1),y:levels[row]})))]}
const positions:Record<string,Point[]>={"3-4-1-2":shape([3,4,1,2]),"3-4-2-1":shape([3,4,2,1]),"3-4-3":shape([3,4,3]),"3-5-1-1":shape([3,5,1,1]),"3-5-2":shape([3,5,2]),"4-1-4-1":shape([4,1,4,1]),"4-2-3-1":shape([4,2,3,1]),"4-3-1-2":shape([4,3,1,2]),"4-3-3":shape([4,3,3]),"4-4-1-1":shape([4,4,1,1]),"4-4-2":shape([4,4,2])};
function chanceClass(value:number|null|undefined){return value==null?"unknown":value>=70?"high":value>=40?"medium":"low"}function chanceLabel(value:number|null|undefined){return value==null?"Dato n/d":`${value}%`}
export function FormationBuilder({team}:{team:Team}){const[index,setIndex]=useState(0),[lineup,setLineup]=useState<Record<string,string>>({}),[message,setMessage]=useState(""),[availability,setAvailability]=useState<Availability|null>(null),formation=formations[index],points=positions[formation.name];useEffect(()=>{fetch(`/api/choices?team=${team.slug}`).then(r=>r.json()).then(data=>{if(data.choice){const i=formations.findIndex(item=>item.name===data.choice.formation);if(i>=0)setIndex(i);try{setLineup(JSON.parse(data.choice.lineup_json??"{}"))}catch{}}});fetch(`/api/availability?team=${team.slug}&v=${Date.now()}`,{cache:"no-store"}).then(r=>r.json()).then(setAvailability).catch(()=>setAvailability({unavailable:true,players:{}}))},[team.slug]);const used=useMemo(()=>new Set(Object.values(lineup)),[lineup]),probability=(player:Player)=>availability?.players[player.name]?.probability??null,score=(player:Player)=>{const p=probability(player);return(p??55)*20+player.fvm*6+player.quotation*2+Math.min(player.cost,100)};function change(i:number){setIndex(i);setLineup({});setMessage("")}function autoFill(){const next:Record<string,string>={},taken=new Set<string>();const ordered=formation.slots.map((slot,i)=>({slot,i,candidates:team.roster.filter(p=>eligible(p.role,slot)).length})).sort((a,b)=>a.candidates-b.candidates);for(const item of ordered){const player=team.roster.filter(p=>!taken.has(p.name)&&eligible(p.role,item.slot)).sort((a,b)=>score(b)-score(a))[0];if(player){next[String(item.i)]=player.name;taken.add(player.name)}}setLineup(next);setMessage(Object.keys(next).length===11?"Formazione ricalcolata: probabilità, FVM, quotazione e compatibilità Mantra.":"Alcuni slot non hanno un ruolo compatibile in rosa.")}async function save(){if(Object.keys(lineup).length<11){setMessage("Completa tutti gli undici slot prima di salvare.");return}const response=await fetch("/api/choices",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({kind:"formation",teamSlug:team.slug,formation:formation.name,lineup})});setMessage(response.ok?"Formazione pubblicata.":"Non sono riuscito a salvare la formazione.")}return <><section className="page-heading formation-heading"><div><p className="eyebrow">Simulatore Mantra</p><h1>Costruisci la formazione</h1><p>Il suggerimento incrocia probabilità, valore e ruoli Mantra.</p><div className="source-strip"><span className={availability?.unavailable?"source-dot off":"source-dot"}/>{availability?(availability.unavailable?"Fonte live non disponibile: uso i valori rosa":`Dati Fantacalcio.it${availability.updated?` · aggiornati ${availability.updated}`:" · aggiornati live"}`):"Aggiornamento probabili formazioni…"}</div></div><div className="formation-controls"><label>Modulo<select value={index} onChange={e=>change(Number(e.target.value))}>{formations.map((item,i)=><option key={item.name} value={i}>{item.name}</option>)}</select></label><button className="secondary-button" onClick={autoFill}>Ricalcola undici</button></div></section><section className="formation-layout"><div className="pitch"><div className="pitch-lines"/><div className="lineup-grid dynamic">{formation.slots.map((slot,i)=>{const current=lineup[String(i)]??"",point=points[i],p=current?availability?.players[current]?.probability:null,tone=chanceClass(p);return <label className={`player-slot ${current?"filled":""}`} style={{left:`${point.x}%`,top:`${point.y}%`}} key={`${formation.name}-${i}`}><span>{slot}</span><select value={current} onChange={e=>setLineup(value=>({...value,[String(i)]:e.target.value}))}><option value="">Seleziona</option>{team.roster.filter(player=>eligible(player.role,slot)&&(!used.has(player.name)||player.name===current)).sort((a,b)=>score(b)-score(a)).map(player=><option key={player.name} value={player.name}>{player.name} · {chanceLabel(probability(player))}</option>)}</select>{current&&<em className={`availability-badge ${tone}`}>{chanceLabel(p)}</em>}</label>})}</div></div><aside className="surface formation-side"><p className="eyebrow">Distinta</p><h2>{team.name}</h2><ol>{formation.slots.map((slot,i)=>{const name=lineup[String(i)],p=name?availability?.players[name]?.probability:null;return <li key={i}><span>{slot}</span><b>{name||"Slot vuoto"}</b>{name&&<small className={chanceClass(p)}>{chanceLabel(p)}</small>}</li>})}</ol><button className="save-button" onClick={save}>Salva formazione</button>{message&&<p className="form-message">{message}</p>}<small>Legenda: turchese ≥70%, oro 40–69%, rosso &lt;40%, grigio dato non disponibile.</small></aside></section></>}

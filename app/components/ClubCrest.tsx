"use client";import{useState}from"react";export function ClubCrest({teamSlug,teamName,size="normal"}:{teamSlug:string;teamName:string;size?:"normal"|"large"}){const[failed,setFailed]=useState(false);const initials=teamName.split(" ").map(x=>x[0]).join("").slice(0,3);return <div className={`club-crest ${size}`}>{failed?<span>{initials}</span>:<img src={`/api/identity/crest?team=${teamSlug}`} alt={`Stemma ${teamName}`} onError={()=>setFailed(true)}/>}</div>}

import {notFound} from "next/navigation";import {requireChatGPTUser} from "../chatgpt-auth";import {Kit3DStudio} from "../components/Kit3DStudio";import {getTeam} from "../data/league";import {teamForEmail} from "../lib/identity";export const dynamic="force-dynamic";export default async function KitPage(){const user=await requireChatGPTUser("/divisa"),slug=teamForEmail(user.email),team=slug?getTeam(slug):null;if(!team)notFound();return <Kit3DStudio team={team}/>}

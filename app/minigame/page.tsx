import{getChatGPTUser}from"../chatgpt-auth";import{AppHeader}from"../components/AppHeader";import{MiniGame}from"../components/MiniGame";import{teamForEmail}from"../lib/identity";export const dynamic="force-dynamic";export default async function Page(){const user=await getChatGPTUser(),slug=teamForEmail(user?.email);return <main className="app-shell"><AppHeader teamSlug={slug} email={user?.email??null}/>{slug?<MiniGame/>:<section className="empty-state"><h1>Squadra non associata</h1><p>Serve un club collegato alla tua email per giocare.</p></section>}</main>}

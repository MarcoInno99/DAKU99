import{notFound}from"next/navigation";import{requireChatGPTUser}from"../chatgpt-auth";import{AppHeader}from"../components/AppHeader";import{CrestUploader}from"../components/CrestUploader";import{getTeam}from"../data/league";import{isAdminEmail,teamForEmail}from"../lib/identity";export const dynamic="force-dynamic";export default async function CrestPage(){const user=await requireChatGPTUser("/identita"),slug=await teamForEmail(user.email),team=slug?getTeam(slug):null;if(!team)notFound();return <main className="app-shell"><AppHeader teamSlug={slug} email={user.email} isAdmin={isAdminEmail(user.email)}/><CrestUploader team={team}/></main>}
